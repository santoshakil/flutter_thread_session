import 'package:flutter/material.dart';

const roundedRectBorder10 =
    RoundedRectangleBorder(borderRadius: BorderRadius.all(Radius.circular(10)));

const listTileTheme = ListTileThemeData(shape: roundedRectBorder10);
